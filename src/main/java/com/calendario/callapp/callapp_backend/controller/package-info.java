/**
 * Capa HTTP del backend.
 *
 * <p>Los controladores exponen la API REST para autenticacion, dashboard,
 * solicitudes, publicaciones, reportes, auditoria, archivos, oficinas,
 * usuarios y catalogos. Cada endpoint delega la logica de negocio a servicios
 * especializados.</p>
 */
package com.calendario.callapp.callapp_backend.controller;
